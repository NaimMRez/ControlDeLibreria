<Window x:Class="LibriGest.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="LibriGest - Inicio de Sesión" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="{DynamicResource MaterialDesign.Brush.Primary}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="BookOpenVariant" Width="64" Height="64" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="LibriGest" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                <TextBlock Text="Sistema de Gestión para Librerías" FontSize="14" Foreground="White" Opacity="0.8" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" Margin="32,24,32,0">
            <TextBox x:Name="TextBoxUsuario" materialDesign:HintAssist.Hint="Usuario"
                     Margin="0,8"/>

            <PasswordBox x:Name="PasswordBoxClave" materialDesign:HintAssist.Hint="Contraseña"
                         Margin="0,8"/>

            <Button Content="INGRESAR" Click="ButtonIngresar_Click" Margin="0,24,0,0"/>

            <TextBlock x:Name="TextBlockError" Foreground="Red" HorizontalAlignment="Center" Margin="0,12,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
