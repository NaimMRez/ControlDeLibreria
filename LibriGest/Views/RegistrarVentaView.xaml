<UserControl x:Class="LibriGest.Views.RegistrarVentaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titulo -->
        <TextBlock Grid.Row="0" Text="Registrar Venta" FontSize="28" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>

        <!-- Barcode + Cliente -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="TextBoxCodigoBarras"
                     materialDesign:HintAssist.Hint="ESCANEAR CÓDIGO DE BARRAS AQUÍ..."
                    
                     Text="{Binding InputCodigoBarras, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="16" Padding="12"
                     PreviewKeyDown="TextBoxCodigoBarras_PreviewKeyDown"/>

            <ComboBox Grid.Column="1" 
                      materialDesign:HintAssist.Hint="Cliente (opcional)"
                      ItemsSource="{Binding Clientes}" DisplayMemberPath="Nombre" SelectedItem="{Binding ClienteSeleccionado}"
                      Margin="12,0,0,0"/>
        </Grid>

        <!-- Grilla de items -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True"
                 >
            <DataGrid.Columns>
                <DataGridTextColumn Header="CÓDIGO" Binding="{Binding CodigoBarras}" Width="120"/>
                <DataGridTextColumn Header="PRODUCTO" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="CANT." Binding="{Binding Cantidad}" Width="80"/>
                <DataGridTextColumn Header="P. UNIT." Binding="{Binding PrecioUnitario, StringFormat='{}{0:C}'}" Width="100"/>
                <DataGridTextColumn Header="TOTAL" Binding="{Binding Total, StringFormat='{}{0:C}'}" Width="100"/>
                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Command="{Binding DataContext.ComandoEliminarItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Foreground="Red"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totales -->
        <Grid Grid.Row="3" Margin="0,12,0,0" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="SubTotal:" FontWeight="Bold" Margin="4"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SubTotal, StringFormat='{}{0:C}'}" HorizontalAlignment="Right" Margin="4"/>

            <TextBox Grid.Row="1" Grid.Column="0" materialDesign:HintAssist.Hint="Descuento" Text="{Binding Descuento}" Margin="4"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Descuento, StringFormat='{}{0:C}'}" HorizontalAlignment="Right" Margin="4"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="TOTAL:" FontSize="18" FontWeight="Bold" Margin="4" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Total, StringFormat='{}{0:C}'}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" Margin="4" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
        </Grid>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CANCELAR" Command="{Binding ComandoCancelarVenta}" Margin="4"/>
            <Button Content="COMPLETAR VENTA" Command="{Binding ComandoCompletarVenta}" Margin="4"/>
        </StackPanel>
    </Grid>
</UserControl>
