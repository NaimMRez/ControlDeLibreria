<UserControl x:Class="LibriGest.Views.GastosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gastos / Caja Chica" FontSize="28" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

        <!-- Estado Caja -->
        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <TextBlock>
                <Run Text="Estado de Caja: "/>
                <Run Text="{Binding CajaAbierta.Estado}" FontWeight="Bold" Foreground="{Binding CajaAbierta, Converter={StaticResource NullToColorConverter}}"/>
            </TextBlock>
        </Border>

        <!-- Formulario -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Concepto" Text="{Binding Concepto}" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Monto" Text="{Binding Monto}" Margin="4,0" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Categoría" Text="{Binding Categoria}" Margin="4,0" Style="{StaticResource MaterialDesignOutlinedComboBox}">
                <ComboBoxItem Content="General"/>
                <ComboBoxItem Content="Servicios"/>
                <ComboBoxItem Content="Transporte"/>
                <ComboBoxItem Content="Limpieza"/>
                <ComboBoxItem Content="Otros"/>
            </ComboBox>
            <TextBox Grid.Column="3" materialDesign:HintAssist.Hint="Comprobante" Text="{Binding Comprobante}" Margin="4,0" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            <Button Grid.Column="4" Content="REGISTRAR" Command="{Binding ComandoRegistrar}" Margin="8,0,0,0" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>

        <!-- Lista -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Gastos}" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="FECHA" Binding="{Binding Fecha, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Width="130"/>
                <DataGridTextColumn Header="CONCEPTO" Binding="{Binding Concepto}" Width="*"/>
                <DataGridTextColumn Header="CATEGORÍA" Binding="{Binding Categoria}" Width="120"/>
                <DataGridTextColumn Header="MONTO" Binding="{Binding Monto, StringFormat='{}{0:C}'}" Width="100"/>
                <DataGridTemplateColumn Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Command="{Binding DataContext.ComandoEliminar, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
