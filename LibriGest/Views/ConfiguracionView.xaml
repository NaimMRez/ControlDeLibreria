<UserControl x:Class="LibriGest.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configuración del Sistema" FontSize="28" FontWeight="Bold" Margin="0,0,0,24" Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="24" MaxWidth="600" HorizontalAlignment="Left">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" materialDesign:HintAssist.Hint="Nombre de la Tienda"
                        
                         Text="{Binding NombreTienda}" Margin="0,4" FontSize="16"/>

                <TextBox Grid.Row="1" materialDesign:HintAssist.Hint="Dirección"
                        
                         Text="{Binding Direccion}" Margin="0,4"/>

                <TextBox Grid.Row="2" materialDesign:HintAssist.Hint="Teléfono"
                        
                         Text="{Binding Telefono}" Margin="0,4"/>

                <ComboBox Grid.Row="3" materialDesign:HintAssist.Hint="Moneda"
                          SelectedItem="{Binding Moneda}" Margin="0,4">
                    <ComboBoxItem Content="S/"/>
                    <ComboBoxItem Content="$"/>
                    <ComboBoxItem Content="€"/>
                </ComboBox>

                <Button Grid.Row="4" Content="GUARDAR CONFIGURACIÓN" Command="{Binding ComandoGuardar}" 
                        Margin="0,16,0,0" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
