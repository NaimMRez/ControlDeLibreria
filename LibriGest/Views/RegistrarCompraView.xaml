<UserControl x:Class="LibriGest.Views.RegistrarCompraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Registrar Compra" FontSize="28" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="TextBoxCodigoBarras"
                     materialDesign:HintAssist.Hint="ESCANEAR CÓDIGO DE BARRAS AQUÍ..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding InputCodigoBarras, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="16" Padding="12"
                     PreviewKeyDown="TextBoxCodigoBarras_PreviewKeyDown"/>

            <ComboBox Grid.Column="1" materialDesign:HintAssist.Hint="Proveedor"
                      ItemsSource="{Binding Proveedores}" DisplayMemberPath="Nombre" SelectedItem="{Binding ProveedorSeleccionado}"
                      Margin="12,0,4,0" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Almacén"
                      ItemsSource="{Binding Almacenes}" DisplayMemberPath="Nombre" SelectedItem="{Binding AlmacenSeleccionado}"
                      Margin="4,0,0,0" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CÓDIGO" Binding="{Binding CodigoBarras}" Width="120"/>
                <DataGridTextColumn Header="PRODUCTO" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="CANT." Binding="{Binding Cantidad}" Width="80"/>
                <DataGridTextColumn Header="P. UNIT." Binding="{Binding PrecioUnitario, StringFormat='{}{0:C}'}" Width="100"/>
                <DataGridTextColumn Header="TOTAL" Binding="{Binding Total, StringFormat='{}{0:C}'}" Width="100"/>
                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Command="{Binding DataContext.ComandoEliminarItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="TOTAL:" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="{Binding Total, StringFormat='{}{0:C}'}" FontWeight="Bold" FontSize="24" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CANCELAR" Command="{Binding ComandoCancelarCompra}" Margin="4" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="COMPLETAR COMPRA" Command="{Binding ComandoCompletarCompra}" Margin="4" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
